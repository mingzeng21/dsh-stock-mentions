# 股票提及

股票提及是把助手已完成 Markdown 中经过证券解析确认的 A 股普通股票文本，呈现为可访问的交互按钮，并提供只读行情面板的功能域。

## 证券

**证券**：本项目中的沪深 A 股普通股票，不包括指数、基金、ETF、债券、B 股、北交所证券或板块代码。

**证券引用**：面板和 Host RPC 使用的规范证券标识，例如 `600519.SH` 或 `000001.SZ`。
_避免_：股票代码、证券名称、任意证券

**规范简称**：证券当前有效的官方简称；曾用名、品牌名、行业别名和模糊匹配不属于规范简称。
_避免_：股票别名、历史名称、模糊名称

## 文本体验

**股票提及**：助手已完成 Markdown 中指向唯一有效证券引用的代码或规范简称。

**文本标注**：原始 Markdown 的半开区间 `[start, end)` 与一个已确认业务对象的纯数据描述；偏移使用 JavaScript UTF-16 code unit。

**候选词**：客户端从已完成助手文本中提取、但尚未由 Host 确认的代码或中文词语。

**标注区域**：普通段落、列表、表格单元格和精确证券代码行内代码中的可见文本；链接、围栏代码块、数学公式、HTML 字面量、reasoning 和工具结果不属于标注区域。

## 数据与界面

**证券解析**：Host 使用固定公开数据源把候选词解析为零个或一个证券引用；零个、多个或类别不符时均视为未解析。

**行情面板**：通过 `shell.overlay` 显示的会话级只读侧栏，包含报价、分时、K 线和个股资讯 tab。

**数据降级**：首选公开数据源不可用后，使用同一数据字段的备用来源；响应记录实际来源和警告，不能把不同口径静默混为同一结果。

**面板状态**：当前会话的临时选中证券、tab 和请求状态；不写入 SessionEvent，也不进入模型上下文。
